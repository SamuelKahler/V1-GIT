# Rollback

1. In GitHub Desktop, open History.
2. Select the commit immediately before `Build graded intelligence evidence engine V4`.
3. Right-click the V4 commit and choose Revert Changes in Commit.
4. Commit the revert and Push origin.

The release does not delete or rewrite source history. Rolling back only removes the new computed intelligence layer and restores the prior UI behavior.
