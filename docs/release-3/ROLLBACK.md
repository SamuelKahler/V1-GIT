# Release 3 rollback

Revert the Release 3 Git commit on `phase-2-mlb-database` and redeploy Preview. No database rollback is needed because this release adds no migration and does not alter stored data.
